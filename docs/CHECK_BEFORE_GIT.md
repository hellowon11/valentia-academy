# 推送到 Git 前的检查清单

## ✅ 代码检查

### 1. 后端代码检查

- [x] `server/database.js` - 数据库配置已创建
- [x] `server/server.js` - 服务器文件已更新，包含：
  - [x] Admin API 端点
  - [x] Excel 导出功能
  - [x] 数据保存功能
- [x] `server/package.json` - 依赖已更新

### 2. 前端代码检查

- [x] `src/pages/Admin/Login.tsx` - 登录页面已创建
- [x] `src/pages/Admin/Dashboard.tsx` - 仪表板已创建
- [x] `src/App.tsx` - 已添加 admin 路由支持
- [x] `vite.config.ts` - 已配置 API 代理

### 3. 配置文件检查

- [x] `vite.config.ts` - 代理配置已添加
- [ ] `.env` 文件 - 确保添加到 `.gitignore`（不要提交敏感信息）
- [x] `server/data/` - 数据库文件目录（应该添加到 `.gitignore`）
- [x] `server/uploads/` - 上传文件目录（应该添加到 `.gitignore`）

## ⚠️ 必须添加到 .gitignore 的文件

创建或更新 `.gitignore` 文件，确保包含：

```
# Environment variables
.env
.env.local
.env.*.local

# Database files
server/data/
*.db
*.db-journal

# Uploaded files
server/uploads/

# Node modules
node_modules/
server/node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
server/dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
```

## 🧪 测试步骤

### 步骤 1: 测试后端服务器

```bash
cd server
npm install  # 如果还没安装依赖
npm start
```

**检查项：**
- [ ] 服务器成功启动在端口 3001
- [ ] 看到 "Database initialized" 消息
- [ ] 看到 "Default admin user created" 消息
- [ ] 没有错误信息

**测试 API：**
- [ ] 访问 http://localhost:3001/api/health
  - 应该返回：`{"status":"OK", ...}`

### 步骤 2: 测试前端服务器

```bash
npm run dev
```

**检查项：**
- [ ] 前端服务器成功启动在端口 5173
- [ ] 没有编译错误
- [ ] 控制台没有警告

### 步骤 3: 测试 Admin 登录

1. 访问：http://localhost:5173/admin/login
2. 使用默认凭据登录：
   - 用户名：`admin`
   - 密码：`admin123`
3. 检查：
   - [ ] 登录页面正常显示
   - [ ] 可以成功登录
   - [ ] 登录后跳转到 dashboard
   - [ ] Dashboard 正常显示

### 步骤 4: 测试 Dashboard 功能

- [ ] 统计卡片正常显示
- [ ] 应用程序列表正常加载
- [ ] 筛选功能正常工作
- [ ] 搜索功能正常工作
- [ ] Excel 导出功能正常（如果有数据）

### 步骤 5: 测试应用程序提交

1. 访问主页：http://localhost:5173
2. 找到一个课程，点击 "Apply Now"
3. 填写并提交申请
4. 检查：
   - [ ] 申请成功提交
   - [ ] 后端控制台显示数据已保存
   - [ ] 在 admin dashboard 中可以看到新申请

## 🐛 常见问题检查

### 问题 1: 后端启动失败

**症状：** `Error: Cannot find module 'better-sqlite3'`

**解决：**
```bash
cd server
npm install
```

### 问题 2: 前端无法连接后端

**症状：** Network error 或 CORS error

**检查：**
- [ ] 后端服务器正在运行
- [ ] `vite.config.ts` 中的代理配置正确
- [ ] 后端 CORS 配置允许前端域名

### 问题 3: 登录失败

**症状：** 401 Unauthorized

**检查：**
- [ ] 后端服务器正在运行
- [ ] 数据库已创建且有默认用户
- [ ] 查看后端控制台的错误信息

### 问题 4: 数据库未创建

**检查：**
- [ ] `server/data/` 目录存在
- [ ] 有写入权限
- [ ] 查看后端启动日志

## 📝 Git 提交前检查

### 必须提交的文件

- [x] `server/database.js`
- [x] `server/server.js`
- [x] `server/package.json`
- [x] `src/pages/Admin/*.tsx`
- [x] `src/App.tsx` (更新部分)
- [x] `vite.config.ts` (更新部分)
- [x] `PRD_Application_Management_Dashboard.md`
- [x] `ADMIN_SETUP.md`
- [x] `QUICK_START.md`

### 不应该提交的文件

- [ ] `.env` 文件
- [ ] `server/data/*.db` 数据库文件
- [ ] `server/uploads/*` 上传的文件
- [ ] `node_modules/` 目录

## 🚀 快速测试脚本

### Windows (PowerShell)

**测试后端：**
```powershell
cd server
npm install
npm start
```

**测试前端（新终端）：**
```powershell
npm run dev
```

## ✅ 最终检查清单

在推送到 Git 之前，确保：

1. [ ] 所有代码已测试，没有错误
2. [ ] `.gitignore` 已更新，排除敏感文件
3. [ ] 后端可以正常启动
4. [ ] 前端可以正常启动
5. [ ] Admin 登录功能正常
6. [ ] Dashboard 可以查看数据
7. [ ] Excel 导出功能正常
8. [ ] 没有敏感信息（密码、密钥等）被提交

## 📋 测试报告模板

```
测试日期：___________
测试人员：___________

后端测试：
- [ ] 启动成功
- [ ] API 响应正常
- [ ] 数据库正常

前端测试：
- [ ] 启动成功
- [ ] 页面加载正常
- [ ] 无编译错误

功能测试：
- [ ] Admin 登录成功
- [ ] Dashboard 显示正常
- [ ] 数据加载正常
- [ ] Excel 导出正常

问题记录：
_______________________________________
_______________________________________
_______________________________________
```

---

**完成后，可以安全推送到 Git！** 🎉
